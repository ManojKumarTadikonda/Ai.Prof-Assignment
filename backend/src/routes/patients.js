import {Router} from 'express';import {Patient} from '../models/index.js';import {auth,roles,tenantScope} from '../middleware/auth.js';const r=Router();r.use(auth);r.get('/',async(req,res,next)=>{try{res.json(await Patient.find(tenantScope(req)).sort({createdAt:-1}).limit(500).lean())}catch(e){next(e)}});r.post('/',roles('PLATFORM_ADMIN','HOSPITAL_ADMIN'),async(req,res,next)=>{try{const data={...req.body};if(req.user.role!=='PLATFORM_ADMIN')data.hospitalId=req.user.hospitalId;res.status(201).json(await Patient.create(data))}catch(e){next(e)}});export default r;
